# 《智能考勤问答 Agent 技术方案》第三轮评审记录：demo 阶段收紧

> **存档说明**：本记录保存本轮提问与 AI 完整评估/执行内容，便于后续复习回顾。
> **评审对象**：`技术方案.md`（751 行修订版 → 本次收紧后 708 行）＋ `requirements.txt` / `requirements-dev.txt`
> **前序记录**：`方案评审记录.md`（第一轮：架构与安全可行性评估）、`方案评审记录-02-深度评审.md`（第二轮：深度评审）
> **评审日期**：2026-09-15

---

## 一、原始提问

> 技术方案.md 评估改进版本 目前只是demo展示阶段 适当收紧 非致命缺陷不做修正方法

（执行原则：① 评估 v3 改进版；② 按 demo 阶段适当收紧文档；③ 非致命缺陷只列入"接受清单"，不展开修正方法。）

---

## 二、v3 改进版评估结论

**第二轮全部关键项已闭环，具备进入编码条件。**

| 第二轮问题 | v3 状态 |
|---|---|
| 用户身份（"我"的指代） | ✅ 闭环：RBAC（`sys_user`/`role`/`user_role` + `auth_node` + 6.4 强制隔离，`user_id` 锚点） |
| 种子时间窗 vs "本月/上周" | ✅ 闭环：种子按运行当月动态生成 + 日期变量注入（`{today}/{month_start}...`） |
| 软删除过滤落地方式 | ✅ 闭环：视图方案（`v_` 前缀 + 校验器拒绝基表，不做 SQL 改写） |
| checkpointer 角色分离 | ✅ 闭环：app_ro / app_rw 双角色 + `.setup()` / autocommit / dict_row 注意事项 |
| sqlglot 解析器 + 检查缺口 | ✅ 闭环：AST 级单语句/只读/白名单/LIMIT；含可写 CTE、SELECT INTO、函数白名单 |
| 罚款等计算归属 | ✅ 闭环：`rules/fines.py` 确定性计算 + 口径单源化（规则库"裁定"条目） |
| 文档内部矛盾（早退罚款、01-10 周六） | ✅ 已修正（罚 10 元对齐 6.3；01-11 周日） |
| 混合意图汇合 | ✅ 已修正（顺序执行单点汇聚） |
| 依赖清单问题 | ✅ 已修正（langchain-deepseek>=1.0.0；checkpoint-postgres>=3.0.0；新增 sqlglot/streamlit；pytest 拆 requirements-dev.txt） |

**v3 新增的加分项**：Web 界面方案（Streamlit 流式 + 登录/RBAC）、延迟与成本预算、执行修复循环、间接注入防护、DoD 与 golden set 规范。

---

## 三、本次"收紧"执行的修订（751 → 708 行）

| # | 修改点 | 处理 | 理由 |
|---|---|---|---|
| 1 | 头部三块版本说明（约束/两轮修正清单） | 合并为「收紧原则 + 基础约束 + 核心设计基线」（20 行 → 11 行） | 去 changelog 冗余，正文已含全部内容 |
| 2 | 4.3 缺席语义长设计 | 压缩为"演示简化"一句 + 指向 6.6 边界 | 非致命项，不展开修正方法 |
| 3 | 4.14 约束清单 | 裁掉非致命项（approver FK、business_trip status、stat_month CHAR(7)、全表规范化），只保 demo 必需 6 项 | 收紧 |
| 4 | 6.5 间接注入 | 简化为"JSON 包裹 + 声明非指令 + 数字溯源"，不展开更多防护 | demo 数据可控，非致命 |
| 5 | 6.6 能力边界 | 升级为「能力边界与演示边界」，新增"**演示阶段接受、不修的已知项**"清单（非致命缺陷集中登记，不修） | 落实"非致命不做修正方法" |
| 6 | 8.5 容错 | 压缩为最小集（执行修复循环 + LLM 三级回退），去掉 DB 重连等 | 收紧 |
| 7 | 十一、Web 界面 | 42 行 → 24 行：去掉选型对比表与部署细节，保留登录/RBAC/流式/进度/引用 | 收紧 |
| 8 | 十三、风险表 | 17 行 → 11 行：已闭环项（双源漂移/周六/混合意图/执行错误/缺席等）从风险表移除 | 风险表只留活风险 |
| 9 | 一致性修正 | 架构图 ⑥ 与 6.2 标题/正文"正则"→"sqlglot AST"；软删除引用与视图方案对齐；技术选型表补 sqlglot 行、只读连接改为双角色 | 消除文档内残留矛盾 |

> 备注：6.1 输入护栏的"规则/正则快筛"属输入侧（非 SQL 校验），保留不改。

---

## 四、演示阶段接受的非致命项（不做修正方法）

1. 周六加班场景不覆盖（种子仅周日用例）；出差审批不校验；
2. 不启用 RLS / 多用户并发（权限走应用层强制，见 6.4）；checkpoint 不清理（TTL 留生产）；
3. 批量数据限额不做专项验证（3 人数据量）；
4. 工资影响类问题（无工资表）走边界话术，不做能力扩展；缺席推断（"谁没来上班"）不支持；
5. 调休余额按简化口径（8h=1 天、按月净额），跨月结转不承诺。

---

## 五、结论与后续

- **结论**：方案已收敛到 demo 可执行状态，无阻塞项，可按 12.2 实施计划从 P0 进入编码。
- **后续原则**：编码期间新发现问题**仅致命才修**（跑不通/讲不通/数据矛盾）；非致命项统一登记到 6.6"演示边界"，不新增设计。
- **演示前检查点**（沿用 DoD）：P0 双角色连通 → P4 端到端 10 问 → P6 golden set ≥90% + 对抗测试 + Web 登录/流式。
