# ============================================================
# 智能考勤问答 Agent 依赖清单
# Python 3.10 | LangChain / LangGraph 1.0+
# 安装：pip install -r requirements.txt
# 开发/测试：pip install -r requirements-dev.txt
# ============================================================

# ---------- 核心框架：LangChain + LangGraph ----------
langchain>=1.0.0
langchain-core>=1.0.0
langchain-deepseek>=1.0.0
langgraph>=1.0.0
langgraph-checkpoint-postgres>=3.0.0

# ---------- PostgreSQL 数据库 ----------
SQLAlchemy>=2.0.30
psycopg[binary]>=3.1.19
psycopg-pool>=3.2.2

# ---------- SQL 校验（AST 级，替代正则） ----------
sqlglot>=25.0.0

# ---------- Web 界面（纯 HTML + FastAPI 静态托管 + SSE） ----------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0

# ---------- 配置与数据校验 ----------
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# ---------- 可选：记忆（按需启用） ----------
# langmem>=0.0.10

# ---------- 未来迭代预留（当前版本不安装） ----------
# 向量语义召回（embedding）——Schema 召回 / 制度混合召回
# sentence-transformers>=3.0.0
# 制度混合召回：BM25 关键字召回
# rank-bm25>=0.2.2
# jieba>=0.42.1
